// Swedish translations are temporarily disabled pending review — toggle hidden
// from the UI. Restore the previous render to re-enable the language switcher.
export function LanguageToggle() {
  return null;
}
